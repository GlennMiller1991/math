import type { INonEmptyArray } from "../type.utils.js";

export class LineInterpolation {

    
    static interpolate(...controlPoints: INonEmptyArray<{controlPoint: number, value: number}>, atPoint: number) {
        
    }

}